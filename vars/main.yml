---
# Shared Variables
# Variables used across multiple roles or global configuration

# Add shared variables here as needed
# Example:
# ansible_user: "{{ lookup('env', 'USER') }}"
# project_root: "{{ ansible_user_dir }}"
