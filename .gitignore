backup/todo
/tmp/*

# Unencrypted vault files (only commit .template or encrypted versions)
vars/*_vault.yml
!vars/*_vault.yml.template

# Semaphore application data (contains secrets and runtime data)
semaphore/config/
semaphore/semaphore/

# SSH test examples
roles/ssh/files/test_examples/

# Worktrees
.worktrees/*
!.worktrees/.gitkeep
.worktrees/
