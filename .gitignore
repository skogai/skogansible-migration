backup/todo
/tmp/*

# Unencrypted vault files (only commit .template or encrypted versions)
vars/*_vault.yml
!vars/*_vault.yml.template

# Semaphore application data (contains secrets and runtime data)
.semaphore/data/

# SSH test examples
roles/ssh/files/test_examples/

# Sensitive files - credentials and secrets
*.key
*.pem
*.p12
*.pfx
.env
.env.local
*.secret
*_vault.yml.decrypted

# Password files
*password*
*secret*
!roles/ssh/vars/example_vault.yml
!*README*
!*example*

# Semaphore sensitive data
semaphore/.env
semaphore/data/

# Backup files that may contain sensitive data
*.backup
*.bak
*~

# Worktrees
.worktrees/*
!.worktrees/.gitkeep
.worktrees/
galaxy_cache
galaxy_token
collections/ansible_collections
.aider*
/roles/skogix
