---
- name: Configure Arch Linux system
  hosts: localhost
  connection: local

  vars_files:
    - vars/main.yml

  roles:
    # - role: system
    #   tags: [system, config]
    # - role: users
    #   tags: [users]
    - role: packages
      tags: [packages, install]
