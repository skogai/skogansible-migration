---
- name: Configure Arch Linux system
  hosts: localhost
  connection: local

  vars_files:
    - vars/main.yml

  roles:
    - role: packages
      tags: [packages, install]

    - role: system-services
      tags: [services, system]
