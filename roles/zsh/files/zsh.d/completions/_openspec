#compdef openspec

# Zsh completion script for OpenSpec CLI
# Auto-generated - do not edit manually

_openspec() {
  local context state line
  typeset -A opt_args

  local -a commands
  commands=(
    'init:Initialize OpenSpec in your project'
    'update:Update OpenSpec instruction files'
    'list:List items (changes by default, or specs with --specs)'
    'view:Display an interactive dashboard of specs and changes'
    'validate:Validate changes and specs'
    'show:Show a change or spec'
    'archive:Archive a completed change and update main specs'
    'status:Display artifact completion status for a change'
    'instructions:Output enriched instructions for creating an artifact or applying tasks'
    'templates:Show resolved template paths for all artifacts in a schema'
    'schemas:List available workflow schemas with descriptions'
    'new:Create new items'
    'set:Set checked-in OpenSpec metadata'
    'workspace:Set up and inspect coordination workspaces'
    'context-store:Set up and inspect context stores'
    'initiative:Create and list coordinated initiatives'
    'feedback:Submit feedback about OpenSpec'
    'change:Manage OpenSpec change proposals (deprecated)'
    'spec:Manage OpenSpec specifications'
    'completion:Manage shell completions for OpenSpec CLI'
    'config:View and modify global OpenSpec configuration'
    'schema:Manage workflow schemas'
  )

  _arguments -C \
    "1: :->command" \
    "*::arg:->args"

  case $state in
    command)
      _describe "openspec command" commands
      ;;
    args)
      case $words[1] in
        init)
          _openspec_init
          ;;
        update)
          _openspec_update
          ;;
        list)
          _openspec_list
          ;;
        view)
          _openspec_view
          ;;
        validate)
          _openspec_validate
          ;;
        show)
          _openspec_show
          ;;
        archive)
          _openspec_archive
          ;;
        status)
          _openspec_status
          ;;
        instructions)
          _openspec_instructions
          ;;
        templates)
          _openspec_templates
          ;;
        schemas)
          _openspec_schemas
          ;;
        new)
          _openspec_new
          ;;
        set)
          _openspec_set
          ;;
        workspace)
          _openspec_workspace
          ;;
        context-store)
          _openspec_context_store
          ;;
        initiative)
          _openspec_initiative
          ;;
        feedback)
          _openspec_feedback
          ;;
        change)
          _openspec_change
          ;;
        spec)
          _openspec_spec
          ;;
        completion)
          _openspec_completion
          ;;
        config)
          _openspec_config
          ;;
        schema)
          _openspec_schema
          ;;
      esac
      ;;
  esac
}

_openspec_init() {
  _arguments \
    '--tools[Configure AI tools non-interactively (e.g., "all", "none", or comma-separated tool IDs)]:value:' \
    '--force[Auto-cleanup legacy files without prompting]' \
    '--profile[Override global config profile (core or custom)]:value:(core custom)' \
    '1::path:_files'
}

_openspec_update() {
  _arguments \
    '--force[Force update even when tools are up to date]' \
    '1::path:_files'
}

_openspec_list() {
  _arguments \
    '--specs[List specs instead of changes]' \
    '--changes[List changes explicitly (default)]' \
    '--sort[Sort order\: "recent" (default) or "name"]:value:(recent name)' \
    '--json[Output as JSON]'
}

_openspec_view() {
  _arguments
}

_openspec_validate() {
  _arguments \
    '--all[Validate all changes and specs]' \
    '--changes[Validate all changes]' \
    '--specs[Validate all specs]' \
    '--type[Specify item type when ambiguous]:value:(change spec)' \
    '--strict[Enable strict validation mode]' \
    '--json[Output validation results as JSON]' \
    '--concurrency[Max concurrent validations (defaults to env OPENSPEC_CONCURRENCY or 6)]:value:' \
    '--no-interactive[Disable interactive prompts]' \
    '1::item-name:_openspec_complete_items'
}

_openspec_show() {
  _arguments \
    '--json[Output as JSON]' \
    '--type[Specify item type when ambiguous]:value:(change spec)' \
    '--no-interactive[Disable interactive prompts]' \
    '--deltas-only[Show only deltas (JSON only, change-specific)]' \
    '--requirements-only[Alias for --deltas-only (deprecated, change-specific)]' \
    '--requirements[Show only requirements, exclude scenarios (JSON only, spec-specific)]' \
    '--no-scenarios[Exclude scenario content (JSON only, spec-specific)]' \
    '(-r --requirement)'{-r,--requirement}'[Show specific requirement by ID (JSON only, spec-specific)]:value:' \
    '1::item-name:_openspec_complete_items'
}

_openspec_archive() {
  _arguments \
    '(-y --yes)'{-y,--yes}'[Skip confirmation prompts]' \
    '--skip-specs[Skip spec update operations]' \
    '--no-validate[Skip validation (not recommended)]' \
    '1::change-name:_openspec_complete_changes'
}

_openspec_status() {
  _arguments \
    '--change[Change name to show status for]:value:' \
    '--schema[Schema override]:value:' \
    '--json[Output as JSON]'
}

_openspec_instructions() {
  _arguments \
    '--change[Change name]:value:' \
    '--schema[Schema override]:value:' \
    '--json[Output as JSON]' \
    '1::artifact:'
}

_openspec_templates() {
  _arguments \
    '--schema[Schema to use]:value:' \
    '--json[Output as JSON]'
}

_openspec_schemas() {
  _arguments \
    '--json[Output as JSON]'
}

_openspec_new() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'change:Create a new change directory'
  )

  _arguments -C \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        change)
          _openspec_new_change
          ;;
      esac
      ;;
  esac
}

_openspec_new_change() {
  _arguments \
    '--description[Description to add to README.md]:value:' \
    '--goal[Workspace product goal to store with the change]:value:' \
    '--areas[Comma-separated affected workspace link names]:value:' \
    '--initiative[Link the repo-local change to an initiative]:value:' \
    '--store[Context store id for --initiative]:value:' \
    '--store-path[Existing local context store root for --initiative]:value:' \
    '--schema[Workflow schema to use]:value:' \
    '--json[Output as JSON]' \
    '1:name:'
}

_openspec_set() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'change:Set repo-local change metadata'
  )

  _arguments -C \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        change)
          _openspec_set_change
          ;;
      esac
      ;;
  esac
}

_openspec_set_change() {
  _arguments \
    '--initiative[Link the repo-local change to an initiative]:value:' \
    '--store[Context store id for --initiative]:value:' \
    '--store-path[Existing local context store root for --initiative]:value:' \
    '--json[Output as JSON]' \
    '1:name:_openspec_complete_changes'
}

_openspec_workspace() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'setup:Set up a workspace and link existing repos or folders'
    'list:List known OpenSpec workspaces'
    'ls:List known OpenSpec workspaces'
    'link:Link an existing repo or folder to a workspace'
    'relink:Update the local path for an existing workspace link'
    'doctor:Check what a workspace can resolve on this machine'
    'update:Refresh workspace-local OpenSpec guidance and agent skills'
    'open:Open a workspace in an agent or VS Code editor'
  )

  _arguments -C \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        setup)
          _openspec_workspace_setup
          ;;
        list)
          _openspec_workspace_list
          ;;
        ls)
          _openspec_workspace_ls
          ;;
        link)
          _openspec_workspace_link
          ;;
        relink)
          _openspec_workspace_relink
          ;;
        doctor)
          _openspec_workspace_doctor
          ;;
        update)
          _openspec_workspace_update
          ;;
        open)
          _openspec_workspace_open
          ;;
      esac
      ;;
  esac
}

_openspec_workspace_setup() {
  _arguments \
    '--name[Workspace name]:value:' \
    '--link[Repo or folder link. Use <path> or <name>=<path>]:value:' \
    '--opener[Preferred opener\: codex-cli, claude, github-copilot, or editor]:value:(codex-cli claude github-copilot editor)' \
    '--tools[Install OpenSpec skills for agents (all, none, or comma-separated tool IDs)]:value:' \
    '--json[Output as JSON]' \
    '--no-interactive[Disable interactive prompts]'
}

_openspec_workspace_list() {
  _arguments \
    '--json[Output as JSON]'
}

_openspec_workspace_ls() {
  _arguments \
    '--json[Output as JSON]'
}

_openspec_workspace_link() {
  _arguments \
    '--workspace[Workspace name from local workspace views]:value:' \
    '--json[Output as JSON]' \
    '--no-interactive[Disable interactive prompts]' \
    '1::name-or-path:_files' \
    '2::path:_files'
}

_openspec_workspace_relink() {
  _arguments \
    '--workspace[Workspace name from local workspace views]:value:' \
    '--json[Output as JSON]' \
    '--no-interactive[Disable interactive prompts]' \
    '1:name:' \
    '2:path:_files'
}

_openspec_workspace_doctor() {
  _arguments \
    '--workspace[Workspace name from local workspace views]:value:' \
    '--json[Output as JSON]' \
    '--no-interactive[Disable interactive prompts]'
}

_openspec_workspace_update() {
  _arguments \
    '--workspace[Workspace name from local workspace views]:value:' \
    '--tools[Select agents for workspace skills-only delivery; global profile selects workflows]:value:' \
    '--json[Output as JSON]' \
    '--no-interactive[Disable interactive prompts]' \
    '1::name:'
}

_openspec_workspace_open() {
  _arguments \
    '--workspace[Workspace name from local workspace views]:value:' \
    '--initiative[Open an initiative as a local workspace view]:value:' \
    '--store[Context store id for --initiative]:value:' \
    '--store-path[Existing local context store root for --initiative]:value:' \
    '--agent[Use an agent for this session\: codex-cli, claude, or github-copilot]:value:(codex-cli claude github-copilot)' \
    '--editor[Open the workspace in VS Code editor mode]' \
    '--prepare-only[Unsupported\: preview surfaces belong to a future context/query command]' \
    '--json[Output as JSON]' \
    '--change[Unsupported\: change-scoped open belongs to future workspace change planning]:value:' \
    '--no-interactive[Disable interactive prompts]' \
    '1::name:'
}

_openspec_context_store() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'setup:Create or register a local context store'
    'register:Register an existing context store directory'
    'unregister:Forget a local context-store registration without deleting files'
    'remove:Forget a local context-store registration and delete its local folder'
    'list:List registered context stores'
    'ls:List registered context stores'
    'doctor:Check local context-store registration and metadata'
  )

  _arguments -C \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        setup)
          _openspec_context_store_setup
          ;;
        register)
          _openspec_context_store_register
          ;;
        unregister)
          _openspec_context_store_unregister
          ;;
        remove)
          _openspec_context_store_remove
          ;;
        list)
          _openspec_context_store_list
          ;;
        ls)
          _openspec_context_store_ls
          ;;
        doctor)
          _openspec_context_store_doctor
          ;;
      esac
      ;;
  esac
}

_openspec_context_store_setup() {
  _arguments \
    '--path[Directory to use for the context store]:value:' \
    '--init-git[Initialize a Git repository in the context store]' \
    '--no-init-git[Skip Git repository initialization]' \
    '--json[Output as JSON]' \
    '1::id:'
}

_openspec_context_store_register() {
  _arguments \
    '--id[Context store id]:value:' \
    '--json[Output as JSON]' \
    '1::path:_files'
}

_openspec_context_store_unregister() {
  _arguments \
    '--json[Output as JSON]' \
    '1:id:'
}

_openspec_context_store_remove() {
  _arguments \
    '--yes[Confirm local context-store folder deletion]' \
    '--json[Output as JSON]' \
    '1:id:'
}

_openspec_context_store_list() {
  _arguments \
    '--json[Output as JSON]'
}

_openspec_context_store_ls() {
  _arguments \
    '--json[Output as JSON]'
}

_openspec_context_store_doctor() {
  _arguments \
    '--json[Output as JSON]' \
    '1::id:'
}

_openspec_initiative() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'create:Create an initiative in a context store'
    'show:Show where an initiative lives and how to read it'
    'list:List initiatives across registered context stores'
    'ls:List initiatives across registered context stores'
  )

  _arguments -C \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        create)
          _openspec_initiative_create
          ;;
        show)
          _openspec_initiative_show
          ;;
        list)
          _openspec_initiative_list
          ;;
        ls)
          _openspec_initiative_ls
          ;;
      esac
      ;;
  esac
}

_openspec_initiative_create() {
  _arguments \
    '--store[Context store id from the local context-store registry]:value:' \
    '--store-path[Existing local context store root]:value:' \
    '--title[Initiative title]:value:' \
    '--summary[Initiative summary]:value:' \
    '--json[Output as JSON]' \
    '1::id:'
}

_openspec_initiative_show() {
  _arguments \
    '--store[Context store id from the local context-store registry]:value:' \
    '--store-path[Existing local context store root]:value:' \
    '--json[Output as JSON]' \
    '1:id:'
}

_openspec_initiative_list() {
  _arguments \
    '--store[Context store id from the local context-store registry]:value:' \
    '--store-path[Existing local context store root]:value:' \
    '--json[Output as JSON]'
}

_openspec_initiative_ls() {
  _arguments \
    '--store[Context store id from the local context-store registry]:value:' \
    '--store-path[Existing local context store root]:value:' \
    '--json[Output as JSON]'
}

_openspec_feedback() {
  _arguments \
    '--body[Detailed description for the feedback]:value:' \
    '1:message:'
}

_openspec_change() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'show:Show a change proposal'
    'list:List all active changes (deprecated)'
    'validate:Validate a change proposal'
  )

  _arguments -C \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        show)
          _openspec_change_show
          ;;
        list)
          _openspec_change_list
          ;;
        validate)
          _openspec_change_validate
          ;;
      esac
      ;;
  esac
}

_openspec_change_show() {
  _arguments \
    '--json[Output as JSON]' \
    '--deltas-only[Show only deltas (JSON only)]' \
    '--requirements-only[Alias for --deltas-only (deprecated)]' \
    '--no-interactive[Disable interactive prompts]' \
    '1::change-name:_openspec_complete_changes'
}

_openspec_change_list() {
  _arguments \
    '--json[Output as JSON]' \
    '--long[Show id and title with counts]'
}

_openspec_change_validate() {
  _arguments \
    '--strict[Enable strict validation mode]' \
    '--json[Output validation results as JSON]' \
    '--no-interactive[Disable interactive prompts]' \
    '1::change-name:_openspec_complete_changes'
}

_openspec_spec() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'show:Show a specification'
    'list:List all specifications'
    'validate:Validate a specification'
  )

  _arguments -C \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        show)
          _openspec_spec_show
          ;;
        list)
          _openspec_spec_list
          ;;
        validate)
          _openspec_spec_validate
          ;;
      esac
      ;;
  esac
}

_openspec_spec_show() {
  _arguments \
    '--json[Output as JSON]' \
    '--requirements[Show only requirements, exclude scenarios (JSON only)]' \
    '--no-scenarios[Exclude scenario content (JSON only)]' \
    '(-r --requirement)'{-r,--requirement}'[Show specific requirement by ID (JSON only)]:value:' \
    '--no-interactive[Disable interactive prompts]' \
    '1::spec-id:_openspec_complete_specs'
}

_openspec_spec_list() {
  _arguments \
    '--json[Output as JSON]' \
    '--long[Show id and title with counts]'
}

_openspec_spec_validate() {
  _arguments \
    '--strict[Enable strict validation mode]' \
    '--json[Output validation results as JSON]' \
    '--no-interactive[Disable interactive prompts]' \
    '1::spec-id:_openspec_complete_specs'
}

_openspec_completion() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'generate:Generate completion script for a shell (outputs to stdout)'
    'install:Install completion script for a shell'
    'uninstall:Uninstall completion script for a shell'
  )

  _arguments -C \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        generate)
          _openspec_completion_generate
          ;;
        install)
          _openspec_completion_install
          ;;
        uninstall)
          _openspec_completion_uninstall
          ;;
      esac
      ;;
  esac
}

_openspec_completion_generate() {
  _arguments \
    '1::shell:(zsh bash fish powershell)'
}

_openspec_completion_install() {
  _arguments \
    '--verbose[Show detailed installation output]' \
    '1::shell:(zsh bash fish powershell)'
}

_openspec_completion_uninstall() {
  _arguments \
    '(-y --yes)'{-y,--yes}'[Skip confirmation prompts]' \
    '1::shell:(zsh bash fish powershell)'
}

_openspec_config() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'path:Show config file location'
    'list:Show all current settings'
    'get:Get a specific value (raw, scriptable)'
    'set:Set a value (auto-coerce types)'
    'unset:Remove a key (revert to default)'
    'reset:Reset configuration to defaults'
    'edit:Open config in $EDITOR'
    'profile:Configure workflow profile (interactive picker or preset shortcut)'
  )

  _arguments -C \
    '--scope[Config scope (only "global" supported currently)]:value:(global)' \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        path)
          _openspec_config_path
          ;;
        list)
          _openspec_config_list
          ;;
        get)
          _openspec_config_get
          ;;
        set)
          _openspec_config_set
          ;;
        unset)
          _openspec_config_unset
          ;;
        reset)
          _openspec_config_reset
          ;;
        edit)
          _openspec_config_edit
          ;;
        profile)
          _openspec_config_profile
          ;;
      esac
      ;;
  esac
}

_openspec_config_path() {
  _arguments
}

_openspec_config_list() {
  _arguments \
    '--json[Output as JSON]'
}

_openspec_config_get() {
  _arguments \
    '1:key:'
}

_openspec_config_set() {
  _arguments \
    '--string[Force value to be stored as string]' \
    '--allow-unknown[Allow setting unknown keys]' \
    '1:key:' \
    '2:value:'
}

_openspec_config_unset() {
  _arguments \
    '1:key:'
}

_openspec_config_reset() {
  _arguments \
    '--all[Reset all configuration (required)]' \
    '(-y --yes)'{-y,--yes}'[Skip confirmation prompts]'
}

_openspec_config_edit() {
  _arguments
}

_openspec_config_profile() {
  _arguments \
    '1::preset:'
}

_openspec_schema() {
  local context state line
  typeset -A opt_args

  local -a subcommands
  subcommands=(
    'which:Show where a schema resolves from'
    'validate:Validate a schema structure and templates'
    'fork:Copy an existing schema to project for customization'
    'init:Create a new project-local schema'
  )

  _arguments -C \
    "1: :->subcommand" \
    "*::arg:->args"

  case $state in
    subcommand)
      _describe "subcommand" subcommands
      ;;
    args)
      case $words[1] in
        which)
          _openspec_schema_which
          ;;
        validate)
          _openspec_schema_validate
          ;;
        fork)
          _openspec_schema_fork
          ;;
        init)
          _openspec_schema_init
          ;;
      esac
      ;;
  esac
}

_openspec_schema_which() {
  _arguments \
    '--json[Output as JSON]' \
    '--all[List all schemas with their resolution sources]' \
    '1::name:_openspec_complete_schemas'
}

_openspec_schema_validate() {
  _arguments \
    '--json[Output as JSON]' \
    '--verbose[Show detailed validation steps]' \
    '1::name:_openspec_complete_schemas'
}

_openspec_schema_fork() {
  _arguments \
    '--json[Output as JSON]' \
    '--force[Overwrite existing destination]' \
    '1:source:_openspec_complete_schemas' \
    '2::name:'
}

_openspec_schema_init() {
  _arguments \
    '--json[Output as JSON]' \
    '--description[Schema description]:value:' \
    '--artifacts[Comma-separated artifact IDs]:value:' \
    '--default[Set as project default schema]' \
    '--no-default[Do not prompt to set as default]' \
    '--force[Overwrite existing schema]' \
    '1:name:'
}

# Dynamic completion helpers

# Use openspec __complete to get available changes
_openspec_complete_changes() {
  local -a changes
  while IFS=$'\t' read -r id desc; do
    changes+=("$id:$desc")
  done < <(openspec __complete changes 2>/dev/null)
  _describe "change" changes
}

# Use openspec __complete to get available specs
_openspec_complete_specs() {
  local -a specs
  while IFS=$'\t' read -r id desc; do
    specs+=("$id:$desc")
  done < <(openspec __complete specs 2>/dev/null)
  _describe "spec" specs
}

# Get both changes and specs
_openspec_complete_items() {
  local -a items
  while IFS=$'\t' read -r id desc; do
    items+=("$id:$desc")
  done < <(openspec __complete changes 2>/dev/null)
  while IFS=$'\t' read -r id desc; do
    items+=("$id:$desc")
  done < <(openspec __complete specs 2>/dev/null)
  _describe "item" items
}

# Use openspec __complete to get available schemas
_openspec_complete_schemas() {
  local -a schemas
  while IFS=$'\t' read -r id desc; do
    schemas+=("$id:$desc")
  done < <(openspec __complete schemas 2>/dev/null)
  _describe "schema" schemas
}
compdef _openspec openspec

