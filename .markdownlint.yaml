# Markdownlint configuration
# Align with yamllint line-length settings (120 chars)

# Default rules
default: true

# Line length - disabled for documentation flexibility
MD013: false

# Allow non-incremental heading levels
MD001: false

# Allow duplicate headings (common in structured docs)
MD024: false

# Allow multiple top-level headings (documentation structure)
MD025: false

# Allow ordered list prefixes in any style
MD029: false

# Allow inline HTML (for role templates, etc.)
MD033: false

# Allow bare URLs
MD034: false

# Allow emphasis as heading (common in structured docs)
MD036: false

# Allow fenced code blocks without language
MD040: false

# Allow first line to not be heading
MD041: false
