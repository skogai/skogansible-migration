# Additional Tool Completions
# Note: compinit is called in 20-completions.zsh
# Note: 1Password completion is cached in 11-1password.zsh
# Note: Homebrew fpath is set in 00-homebrew.zsh with static paths

# This file is kept for any additional completion setup that needs
# to run after the main completion system is initialized
